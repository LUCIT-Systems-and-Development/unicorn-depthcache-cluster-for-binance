apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lucit-ubdcc.fullname" . }}-test-connection"
  labels:
    {{- include "lucit-ubdcc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lucit-ubdcc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
