Todo:
- Inteligente Distribution! Count subs / pod
- Sync DNC last restart time
- Count subs / pod


Ideas:
- restapi: Add error if result is OK but one or mor DCNs did not response
- use websocket between client und restapi instead of rest
- Replace JSON lib?
- self.db.manage_distribution() in create_depthcache(s) optional?